				DCM '96

1. OS (10) 		2. RDBMS (10)	3. Networking(10)

1. Searching is increased to 
Possible answer : No. of records are less
2. Normalisation is used in which case 
a.  Design 	b. Coding      c. Testing        d. None of These
3. Join operation takes 
Ans:  Two relations as operands
4. Basic unit of relation is
a. Table	b. Row	c. Column
5. RDBMS is based on the concept of
a. Referential Integrity (This may be the answer)
6. Question about foreign key concept
7. DML is used for 
a. Accessing data from database
8.    
	Ans:   Parser, Lexical, Generation code
9. Memory managing scheme used in DOS is 
Segmentation
10. Unknown

OS.
1. ?'rnm oldfile newfile'  will do
a. Delete old file
b. Delete new file
c. Rename old file
d. Rename new file
2. 'echo' will do
a. Print all files in current directory
3. UNIX uses 
a. Demand paging

4.    Which of the following is NOP operator   
       a. CLD	b. Push PS  Pop PS
5. If DOS has abc.com ,abc.exe and abc.bat files then writing abc at prompt (which of the files executes 
First)
Ans: firtst com,abc.exe   and then abc.bat
6. Semaphore is used for:
Ans: Mutual exclusion
7. I-node refers to:
Ans: Index Node
8. ? ans: lexical  analyser,parser,lexical generation code.


Networking

1. ?fi is:
        Ans :email 
2. ATM stands for:
Ans:  Asynchronous transfer mode
3. Modems are used to transefer:
Ans: Digital to analog
4. Intranetworking is used in:
5. Telnet is used for:
Ans : Remote login
6. Packet information taken in:
Ans:  Network layer
7. TCP/IP uses which protocol:


C problems  
1. get(b,n)
int b,n;
{
   If(i*(b<<7)>>(n%8))
       Return 1;
Else
       Return 0;
}
main()
{
      int c= 0x40,I;
      for (I=0;I<8;I++0)
      {
             if(get(c,I))
                  printf("*");
            else
                 printf("+");
     }
ans:  +******+

2. #include<stdio.h>
char* ptr="dcm";
main()
{
    int ch;
   while(ch = *ptr++)
    {
        ch = ch+'a'-a;
         printf("%d",ch);
    }
     }
ans: 100,99,109

3. int I=0;
       char* ptr = "lion\0ABCD";
main()
{
     int c;
      while((c =*ptr++)!=0)
          I++;
       Printf("%d",I);
       }
ans: 4


4. int I=2,j=3,k;
k=I++ + j;
printf("%d %d %d",I,j,k);

ANS:3,3,5
5. compute (int m,int n)
{
      if(m==0)
            return (n+1);
      else
       {
             if(n==0)
                   compute(m-1,1);
            else
                   compute(m-1,compute(m,n-1);
         }
}
main()
{
      I=compute(2,3)
      Printf("%d",I);
}
6. int I,j,num=1;
main()
{
   for(I=0,I<=100,I++)
    {
           for(j=0;j<=100;j++)
                 num+= 1;
    }
  printf("5d",num);
}


ans:10202

*********************************************************
The Questions are :

1. semaphores ,used for ?
2. SNAP : full form ?
3. TFTP : full form ?
4. thrashing causes what ? system underutilization
5. Rollback in databases ?
6. Rollforward in databases ?
7. memory management in Dos 6.2 ?
8.   "       "        " Win NT ?
8.  IPV6 : packet size , address size ?
9.  what is CREATE & SELECT : DML or DDL ?
10. what is the difference between
         select count empno
         and select count *
11. 2 SQL queries ? (noone could remember.)
12. difference between primary key/unique key ?
13. foreign key : duplications allowed or not ?
14. Java is it a interpreter or an internet version of C++
15. ethernet is which of the following
      ans : 10BT,x.25,802.3
16. DOS hard disk
         there were 4 choices,all permutations of
         partition table,boot sector,fat,directory

       you are supposed to identify the correct sequence in which
       the 4 are present ?
17. what is the correct sequence of a complier operations ?
     lex,symbol table,parsing
18. A question on LES : command in assembly ?

  10 big questions on c . noone could remember those .
  No negative marking in c paper .

otherwise +1 for a correct ans. and a -1 for a wrong answer.






 