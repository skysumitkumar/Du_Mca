NOVELL's Written Test :
It contained three sections :

	1. Aptitude.
	2. General CS concepts (mainly OS and Arch).
	3. C Programming.

Here are the questions from the third section.  Three of them are
missing.

char mychar()
{
	return 'c';
}
main()
{
	/*
	printf("%d\n", (1, 2, 3, 4, 5));

	What is the output?
	*/

	/*
	int i = 3;
	switch (i) {
		default : printf("welcome\n");
		case 1 : printf("to\n");
		case 2 : printf("novell\n");
		case 3 : printf("india\n");
	}
	What is the output ?
	*/

	/*
	int i;
	for(i=0;i<3;i++, printf("%d ", i));
	What is the output ?
	*/

	/*
	int	i;
	char	s[10];
	for (i = 0; i < 4; i++)
		s[i] = 'a';
	printf("%s", s);

	What is the output ?
	*/

	/*
	int	index, flag;

	index = 0;
	flag = 0;
	while (index < 10 || !flag) {
		printf("hello %d\n", index);
		index = index + 1;
		if (index < 5)
			flag = 1;
		else
			flag = 0;
	}
	/* How many times is "hello" printed? * /
	*/

	/*
	int	a = 100, b;

	if (b=a, a+b, a-b)
	if (b == 100)
	printf("b = 100\n");else
	if (a != b)
	printf("a != b\n");
	printf("a = b\n");

	What is the output ?
	*/

	/*
	int	score = 5;
	if (score = 20)
		printf("twenty\n");
	else if (score == 10)
		printf("ten\n");
	else if (score == 5)
		printf("five\n");
	else
		printf("zero\n");

	What is the output ?

	*/

	/*
	int	a;

	a = 0 + 10 + 010;
	printf("%d\n", a);

	What is the output ?
	*/

	/*
	int	x = 1, y = 2;

	printf("%d %d\n", (x&y), (x && y));

	What is the output ?
	*/

	/*
	char* cpt1, cpt2;
	int	c;

	c = mychar();
	cpt1 = mychar();
	cpt2 = mychar();
	return 0;

		Which is invalid ?
		(a) c = mychar();
		(b) only cpt2 = mychar();
		(c) only cpt1 = mychar();
		(d) both (b) and (c)
	*/

	/*
	char str[] = "welcometonovell";
	char *ptr;

	ptr = &str[2];
	ptr += 5;
	printf("%c", *ptr);

	What is the output ?

	*/

	/*
	int *(*ptr)(int *);
	What is ptr? (describe in words)
	*/

	/*
	int	i = 100;
	{
		int	i;

		i = 200;
		{
			int	i;

			i = 300;

			printf("%d", i);
			i = 500;
		}
		printf("%d", i);
	}

	What is the output ?
	*/
}
